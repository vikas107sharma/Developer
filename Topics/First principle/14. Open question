1 How to upload a 1 GB file on S3 
Steps:
 backend creates a CreateMultiPartUpload upload and it decodes chunks lets say it comes out to be 21 then it returns 21 presigned url to frontend 
 frontend uploads on those urls by adding retries lets say 5th chunk is failed so it will retry only that part 
 alfter successful uploa it tells backend that it is done then backend calls aws (completeMultipartUpload )

2. Https is secure from https how ?
In http data transfer in raw text -> not encrypted -> not secure 

In https  
Data sent over a secured channel -> encrypted -> ore secure 
how tls handshake works ?

