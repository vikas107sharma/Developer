✅✅✅✅✅✅✅✅✅✅ Database ✅✅✅✅✅✅✅✅✅✅
Atomicity:
  Transactions all or nothing

Consistency:
  DB moves from one valid move to other
  Constraints are always respected

Isotation:
  Transactions do not see each other

Durability:
  Once commited it stays forever

======================== Isolation Problems ========================

Dirty read:
  Read uncommitted data of another transaction
  T1: update account set balance = 50
  T2: reads 50
  T1: Rollback
  Now balance is actually 100 but T2 reads 50

Non repeatable read:
  Same row reads twice -> different value
  T1: select balance -> 100
  T2: update balance = 150; commit
  T1: select balance -> 150

Phantom read:
  New rows appears of disappears between queries
  T1: select * from orders where amount > 100 (returns 5 rows)
  T2: insert order(amount=200)
  T1: select again -> returns 6 rows

Lost update:
  Balance is 100
  T1: reads 100
  T2: reads 100

  T1 updates -> 80
  T2 updates -> 70 (overwrites T1)

======================== Database Indexes ========================

Indexes are data structures that help databases find rows faster without scanning the full table.

Internally, indexes map column values to row locations.

Example:
a@gmail.com -> row_id 1523

Most databases use:
B-Tree indexes
for efficient searching and sorting.

======================== Composite Index ========================

INDEX(user_id, created_at)
Leftmost Prefix Rule

Works for:
user_id
user_id + created_at

Does NOT work efficiently for:
created_at only

because indexes are ordered from left to right.

======================== Why Indexes Slow Down Writes ========================

Indexes improve reads but increase write cost.

When inserting/updating rows:
table data updates
all related indexes also update

Example:
INSERT row
-> update table
-> update indexes

More indexes:
slower writes
higher storage usage
more memory consumption

======================== When Indexes Are Not Used ========================
1. Low Cardinality Columns

Columns with very few unique values are poor candidates for indexing.

Example:
gender:
M -> 60%
F -> 40%
DB may prefer full table scan because index selectivity is poor.

2. Using Functions on Indexed Columns
Example:
WHERE LOWER(email) = 'a@gmail.com'

Applying functions can prevent normal index usage because indexed values are transformed during query execution.

3. Leading Wildcards
LIKE '%abc%'

B-Tree indexes cannot efficiently search from the middle of a string.

Works better:
LIKE 'abc%'
because index traversal can start from prefix order.

======================== Indexing Strategies ========================
Which queries are frequent ?
Which columns appear in where ?
Which columns appear in JOIN ?
Which columns appear in ORDER BY ?

======================== Physical Storage of Index ========================

Indexes are stored separately from table data.

In most relational databases:

Table rows -> Data Pages
Indexes -> Index Pages

Stored on:

SSD/HDD
frequently accessed parts cached in RAM

======================== How DB Uses an Index ========================

Query execution flow:

Check if matching index exists
Traverse B-Tree
Root -> Internal Nodes -> Leaf Node
Find row pointer
Fetch actual row from table

======================== Normalization ========================
1NF
Atomic/single values only.

2NF
No partial dependency on composite key.

3NF
No transitive dependency.

Over Normalization
Too many joins
Slow read performance
Complex queries

======================== How DB Executes a JOIN ========================
1. Nested Loop Join
For each row in table A, DB scans matching rows in table B.
Slow without indexes.

2. Index Nested Loop Join (Most Common)
For each row in table A:
Use index on table B
Very efficient when join columns are indexed.

======================== Best Practices for JOIN ========================
Index foreign keys
Avoid SELECT *
Join smaller datasets first

======================== JOIN vs Denormalization ========================

Many joins:
Normalized -> slower reads

Denormalization:
Duplicated data -> faster reads

Real systems usually:
Normalize core data
Denormalize hot/read-heavy paths

Example:
Movies + Theatres + Shows + Seats

Heavy joins can be optimized using:
cached responses
pre-joined views
read models
